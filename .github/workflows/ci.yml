name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: [self-hosted, Linux, ARM64]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust 1.94.0 toolchain
        uses: dtolnay/rust-toolchain@1.94.0

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Create empty assets/ directory (model.safetensors not in CI)
        run: mkdir -p assets

      - name: Run library unit tests
        run: cargo test --lib

      - name: Verify Rust API example compiles
        run: cargo check --example api_usage

      - name: Run integration error-handling test (no model assets required)
        run: cargo test --test integration_tests test_missing_model_file_returns_model_load_error

  lint:
    name: Lint
    runs-on: [self-hosted, Linux, ARM64]

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust 1.94.0 toolchain with components
        uses: dtolnay/rust-toolchain@1.94.0
        with:
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-lint-

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy (warnings as errors)
        run: cargo clippy --all-targets -- -D warnings
